settings:
  namespace_roots:
    code/src: Src
    code/tests: Tests
  source_extension: ".js"
  test_suffix: ".spec"
  test_extension: ".js"
  namespace_separator: "."

coverage_xml: code/clover.xml
min_coverage: 80

structure:
  - name: "Mocha sample"
    paths:
      source: "code/src"
      test: "code/tests"
    rules:
      - minimum-coverage:
          min: 80
